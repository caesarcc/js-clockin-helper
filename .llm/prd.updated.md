# PRD - Atualizado — Calculadora de Jornada de Trabalho

Este arquivo é uma versão estendida do PRD original, contendo detalhes técnicos adicionais para PWA, deploy, testes, acessibilidade e schema de configuração.

Principais mudanças aplicadas:

- Preferência por UI leve (Tailwind) para reduzir bundle.
- `manifest.json` e `service-worker.js` de exemplo adicionados em `/public`.
- `config.schema.json` adicionado na raiz para validar `config.json`.
- Breakpoints, requisitos de acessibilidade e recomendações de performance documentados.
- Workflow de deploy para GitHub Pages criado em `.github/workflows/deploy.yml`.

Arquivos adicionados:

- `public/manifest.json`
- `public/service-worker.js`
- `config.schema.json`
- `.github/workflows/deploy.yml`
- `assets/icons/README.md` (instruções para ícones)

Se quiser, eu posso:

- Atualizar o arquivo PRD original (`.llm/prd.md`) substituindo pelo conteúdo desta versão atualizada.
- Gerar os componentes iniciais (Vite + React + Tailwind) e testes automatizados.
- Comitar e abrir um PR com as alterações.

Indique qual próximo passo prefere.
